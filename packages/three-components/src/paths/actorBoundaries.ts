import { createBoundary } from '../utils';

export const spongebobBoundary = createBoundary(6, 6);

export const squidwrdBoundary = createBoundary(6, 6);

export const patrickBoundary = createBoundary(6, 6);
