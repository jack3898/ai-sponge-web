export const container = document.querySelector('.root-container') as HTMLBodyElement;
export const canvas = document.querySelector('.root-canvas') as HTMLCanvasElement;

export const containerWidth = () => container.offsetWidth;
export const containerHeight = () => container.offsetHeight;
